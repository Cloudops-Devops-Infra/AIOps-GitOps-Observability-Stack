{
  "name": "jenkins-serverless-react-cicd",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p build && echo '<h1>🚀 Deployed Successfully via Docker Compose AIOps Stack!</h1>' > build/index.html"
  },
  "dependencies": {}
}